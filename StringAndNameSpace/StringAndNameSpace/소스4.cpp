#include <cstdio>
#include <vector>
#include <string>

using namespace std; // 이 코드가 나온 시점부터 전부다 std라는 네임스페이스를 생략하겠다.

int main() {
	string myStr = "Hello";

	vector<int> myvec;

	printf("%s\n", myStr.c_str());

	return 0;
}