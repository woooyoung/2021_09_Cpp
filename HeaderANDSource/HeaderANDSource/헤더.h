#include <cstdio>
#include <cmath>  // sin(), cos(), tan(), exp(), pow(), log()
#include <cstdlib> // malloc, free, rand
#include <ctime> // 의사난수 시드 설정


struct Person
{
	int age;
	char name[100];
};

int add(int a, int b);